<!DOCTYPE html>
<html>
<head>
  <title>Membership Details</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" />
  <style>
    body {
      background-color: #0c0f18;
      color: white;
      font-family: Arial, sans-serif;
      padding: 40px 20px;
    }

    .membership-container {
      max-width: 900px;
      margin: auto;
      text-align: center;
    }

    table {
      width: 100%;
      margin-top: 30px;
      background-color: #1a1d29;
      border-radius: 10px;
      overflow: hidden;
    }

    th, td {
      padding: 16px;
      border: 1px solid #ffffff1a;
    }

    th {
      background-color: #2b2f40;
      color: #ffc107;
    }

    tr:nth-child(even) {
      background-color: #1f2233;
    }

    tr:hover {
      background-color: #2e3347;
    }

    .current-tier {
      margin-top: 40px;
      font-size: 1.2rem;
    }

    .expiry-date {
      margin-top: 10px;
      font-size: 0.9rem;
      color: #ffc107;
    }

    .btn-manage {
      margin-top: 20px;
      padding: 12px 24px;
      font-size: 16px;
      background-color: #f49b00;
      color: white;
      border: none;
      border-radius: 8px;
      transition: background-color 0.3s ease;
      text-decoration: none;
      display: inline-block;
    }

    .btn-manage:hover {
      background-color: #d88700;
      color: white;
    }

    .lounge-note {
      margin-top: 20px;
      font-size: 14px;
      color: #ffc107;
      font-style: italic;
    }
  </style>
</head>
<body>
  <%- include('partials/Membernav.ejs', { member: member, currentTier: currentTier }) %>

  <div class="membership-container">
    <h1 class="mb-4">Membership</h1>

    <table class="table table-bordered table-dark text-center">
      <thead>
        <tr>
          <th>Tier</th>
          <th>Price (one-time)</th>
          <th>Cashback</th>
          <th>Discount</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>Gold</td>
          <td>$39.99</td>
          <td>5%</td>
          <td>5%</td>
        </tr>
        <tr>
          <td>Silver</td>
          <td>$24.99</td>
          <td>3%</td>
          <td>3%</td>
        </tr>
        <tr>
          <td>Bronze</td>
          <td>Free</td>
          <td>0%</td>
          <td>0%</td>
        </tr>
      </tbody>
    </table>

    <div class="info-note">
      <p><strong> Silver tier members can upgrade to Gold by paying an additional $15.</strong></p>

    <div class="lounge-note">
      *Gold tier members have EXCLUSIVE access to the club's premier lounge.
    </div>

    

    <div class="current-tier">
      <strong>Your membership:</strong> <span class="text-warning"><%= currentTier %></span>
    </div>

    <% if (typeof expiryDate !== 'undefined' && expiryDate && expiryDate !== '0000-00-00') { %>
      <div class="expiry-date">
        <small>Expires on: <%= new Date(expiryDate).toLocaleDateString('en-US', { 
          year: 'numeric', 
          month: 'long', 
          day: 'numeric' 
        }) %></small>
      </div>
    <% } else if (currentTier !== 'Bronze') { %>
      <div class="expiry-date">
        <small>Expiry date not set</small>
      </div>
    <% } %>

    <a href="/managemembership" class="btn-manage">
      Manage Membership
    </a>
  </div>
</body>
</html>