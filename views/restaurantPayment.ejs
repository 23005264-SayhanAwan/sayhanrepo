<!DOCTYPE html>
<html>
<head>
  <title>Restaurant Payment</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" />
  <style>
    body { background-color: #0c0f18; color: white; }
    .summary-box {
      background-color: #1c1f2e;
      padding: 30px;
      border-radius: 10px;
      margin-top: 50px;
    }
    .btn-success {
      background-color: #28a745;
      border: none;
    }
  </style>
</head>
<body>
  <%- include('partials/Membernav', { member: member }) %>

  <div class="container summary-box">
    <h1 class="text-warning text-center mb-4">Payment Summary</h1>

    <form action="/restaurant/confirm-payment" method="POST">
      <input type="hidden" name="total" value="<%= total %>">
      <input type="hidden" name="cashbackUsed" value="<%= cashbackUsed %>">
      <input type="hidden" name="finalAmount" value="<%= finalAmount %>">
      <input type="hidden" name="descriptions" value="<%= descriptions %>">
      <input type="hidden" name="imageURLs" value="<%= imageURLs %>">

      <p><strong>Total:</strong> $<%= total.toFixed(2) %></p>
      <p><strong>Cashback Used:</strong> $<%= cashbackUsed.toFixed(2) %></p>
      <p><strong>Final Amount to Pay:</strong> $<%= finalAmount.toFixed(2) %></p>

      <div class="text-center mt-4">
        <button type="submit" class="btn btn-success btn-lg">Pay Now</button>
      </div>
    </form>
  </div>
</body>
</html>
