<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Membership Upgraded</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" />
  <style>
    body {
      background-color: #0c0f18;
      color: white;
      text-align: center;
      padding-top: 100px;
    }
    .success-box {
      background-color: #1c1f2e;
      padding: 40px;
      border-radius: 12px;
      display: inline-block;
      margin-top: 40px;
    }
    .btn-warning {
      background-color: #f49b00;
      border: none;
      padding: 10px 20px;
      border-radius: 8px;
      font-size: 16px;
    }
    .btn-warning:hover {
      background-color: #d88700;
    }
  </style>
</head>
<body>
  <%- include('partials/Membernav', { member: member }) %>

  <div class="success-box">
  <h1 class="text-success">🎉 Membership Updated!</h1>
  <% if (newTier === 'Bronze') { %>
    <p>Your membership has been downgraded to <strong>Bronze (Free)</strong>.</p>
    <p>You can upgrade again anytime!</p>
  <% } else { %>
    <p>Your membership has been upgraded to <strong><%= newTier %></strong>.</p>
    <p>Enjoy your premium benefits!</p>
  <% } %>
  <a href="/membership" class="btn btn-warning mt-3">Back to Membership Page</a>
</div>
</body>
</html>
